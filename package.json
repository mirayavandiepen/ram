{
  "name": "kerned-workspace",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "pnpm --filter kerned build && pnpm --filter docs build",
    "dev": "pnpm --parallel --filter kerned --filter docs dev",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  }
}
